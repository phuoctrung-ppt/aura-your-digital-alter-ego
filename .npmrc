# Hoist for Expo / React Native native module resolution in monorepos.
# Documented: required for many Expo packages to resolve peer deps correctly.
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
public-hoist-pattern[]=*
