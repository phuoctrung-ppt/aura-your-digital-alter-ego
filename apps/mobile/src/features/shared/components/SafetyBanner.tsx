/**
 * Thin re-export — canonical implementation lives under session/safety (M10).
 * Keeps historical import paths (`features/shared`) compiling.
 */
export { SafetyBanner } from "../../session/safety";
