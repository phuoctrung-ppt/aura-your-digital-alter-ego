# Copy to docker/.env if you want non-default local credentials.
# Safe local-dev defaults are already baked into docker-compose.yml.
# NEVER put production secrets here.

POSTGRES_USER=aura
POSTGRES_PASSWORD=aura
POSTGRES_DB=aura
POSTGRES_PORT=5432

OLLAMA_PORT=11434
